You will need Postgres and Go installed to run the program.

To install the gator CLI, use the command "go install github.com/BlessE1/gator"

To set up the config file in your home directory, you will need a database url (db_url).

A few commands are agg, register and browse.
